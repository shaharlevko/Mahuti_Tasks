# Production Environment Variables for Render
# Copy these values to Render environment variables settings

# Supabase Configuration (required)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# JWT Secret (required - generate a strong random secret)
JWT_SECRET=your-production-jwt-secret-min-32-chars

# Session Secret (required - generate a strong random secret)
SESSION_SECRET=your-production-session-secret

# Frontend URL (required - your Vercel frontend URL)
FRONTEND_URL=https://mahuti-tasks.vercel.app

# Google OAuth (optional)
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
GOOGLE_CALLBACK_URL=https://your-backend.onrender.com/api/auth/google/callback

# Email Configuration (optional, for invitations)
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_app_password

# Server Configuration
PORT=3001
NODE_ENV=production
